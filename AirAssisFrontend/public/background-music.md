# Background music asset

Add a licensed `background-music.mp3` file directly in this `public` folder.

The application serves it as `/background-music.mp3`. The play button in the lower-right corner starts and pauses playback after the visitor chooses to enable it.
